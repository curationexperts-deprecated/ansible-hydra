- hosts: localhost
  name: create
  connection: local
  gather_facts: False
  roles:
    - launch_ec2

- hosts: newboxes
  name: setup
  user: ubuntu
  gather_facts: true
  roles:
    - { role: packages }
    - { role: ec2 }

- include: configure.yml hosts=newboxes
